class Solution {
public:
    int findContentChildren(vector<int>& g, vector<int>& s) {
        if (s.size()==0 or g.size()==0){
            return 0;
        }
        sort(g.begin() , g.end());
        sort(s.begin(), s.end());
        int x = g.size()-1;
